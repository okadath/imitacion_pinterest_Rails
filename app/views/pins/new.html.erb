<div class="container">
	<% @pin.user_id %>
	<%= @pin.user_id %>
	meh
	<%=params[:user_id] %>
	<%=params[:name] %>
	<%= "mh" %>

	<h2>Agregar nuevo pin</h2>
	<div class="row">
		<div class="col-md-6">
		<%= render 'form', pin: @pin %>
		</div>
	</div>
<p><%= link_to 'Back', pins_path %>

</div>
